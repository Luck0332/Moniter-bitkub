services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - db_data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/app/data/liberix.db
    restart: unless-stopped

volumes:
  db_data:
